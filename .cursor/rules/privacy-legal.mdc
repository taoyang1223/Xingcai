---
description: 所有技术开发必须保护客户隐私并遵守中国相关法律，冲突时隐私优先于功能
alwaysApply: true
---

# 隐私与合法（开发前置）

后续一切代码、接口、表结构、日志、模型训练、第三方接入，都必须保护客户隐私，遵守《个人信息保护法》《数据安全法》《网络安全法》及 App 违法违规收集认定方法。细则见 `智能量体试衣/技术文档/06-安全与合规设计.md` 与文档 10 的 3.1 / 4.5。

**功能与隐私冲突时，停功能，不打折扣隐私。** 不得以「先跑通再补合规」合入。

## 必须做到

- 写身体数据前校验单独同意；无 `consent_id` 直接拒绝。
- 围度走统一加解密封装；业务库只存密文。
- 照片：端侧糊脸与拦截 → 短存桶直传 → 建模完成即删（≤1 小时兜底）。业务库不存原图。
- 资源访问校验归属，防 IDOR。
- 用户可删档案、可撤回同意、可注销（含备份承诺周期）。

## 禁止

- C 端采集或入库真实姓名、身份证、证件照、人脸核身。`users` 不得加 `real_name` / `id_card`。
- 日志、埋点、Issue、聊天里出现围度数字、照片 URL、完整手机号。
- 默认把用户原图或可识别体型拿去训练（须单独勾 `body_data_train`）。
- 绕过 `MeasureProvider` 把照片交给厂商；厂商须 DPA：不留存、不训练、不出境。
- 分享试穿图携带围度；后台未审批查看身体明文。

```go
// ❌ BAD
log.Infow("measured", "chest", chest, "phone", phone)

// ✅ GOOD
log.Infow("measure_job_done", "job_uid", uid, "user_uid", userUID)
```
